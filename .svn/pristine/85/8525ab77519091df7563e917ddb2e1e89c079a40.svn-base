/**
 * 
 */
package models.configuracion.generales.precios_recompra;

import models.msc_utilitys.MSCModelExtend;
/**
 *Clase que se encarga de Confirmar si se desea eliminar un registro de precio recompra titulo tb_120
 */
public class PreciosRecompraConfirm extends MSCModelExtend{
@Override
	public void execute() throws Exception {
	storeDataSet("precio_recompra",getDataSetFromRequest());
	}
}
